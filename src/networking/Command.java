package networking;

/**
 * Comamnd Enum which indicates the type of message being sent across the server.
 * @author aaquibnaved
 *
 */
public enum Command {
	SESSION, GAME, MESSAGE, SEND_ID, QUIT, NULL
}
